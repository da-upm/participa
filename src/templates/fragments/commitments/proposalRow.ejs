<td><input type="checkbox" class="form-check-input proposal-checkbox" name="proposalIds" value=<%= proposal._id.toString() %> <%= proposal.selected ? 'checked' : '' %>></td>
<td>
    <%= proposal.title %>
</td>
<td class="commitment-column">
Vacío
<td class="actions-column">
    <button class="btn btn-link view-more" hx-get="/api/commitments/proposals/<%= proposal._id.toString() %>"
        hx-target="#proposalDetailModal" hx-on::after-request="if(event.detail.xhr.status === 200) $('#proposalDetailModal').modal('show')">Abrir</button>
</td>